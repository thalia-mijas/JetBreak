import { Client } from '../Client';
import { Resource } from '../Resource';
import { DuffelResponse } from '../types';
import { ComponentClientKey, ComponentClientKeyPayload } from './IdentityTypes';
export declare class ComponentClientKeys extends Resource {
    /**
     * Endpoint path
     */
    path: string;
    constructor(client: Client);
    /**
     * The client key to authenticate Duffel UI components.
     */
    create: (data?: ComponentClientKeyPayload) => Promise<DuffelResponse<ComponentClientKey>>;
}
